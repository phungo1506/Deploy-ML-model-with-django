import pytest
"""
    This test case is created for the purpose of performing the demo tool CircleCI.
    No effect in testing the project.
    Github is missing an important file. It is file model (yolov4.weights) in folder models, our team can't upload it to github because of the size limit of github
"""
def func(x):
    return x + 5
def test():
    assert func(4) == 9

